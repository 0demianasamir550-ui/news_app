import 'package:flutter/material.dart';

class AppColors {
  static const Color whiteColor = Color(0xFFFFFFFF); // أبيض صحيح
  static const Color blackColor = Color(0xFF171717); // أسود غامق (مش أحمر!)
  static const Color grayColor = Color(0xFFA0A0A0);  // رمادي (توافق مع app_styles)
  static const Color primaryLight = Color(0xFF3EA0FF); // أزرق فاتح (المفقود)
  static const Color transparentColor = Colors.transparent;
}